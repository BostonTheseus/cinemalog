<div class="booyah-box col-10 offset-1">
  <h1><%= @movie.title %></h1>
  <i><%= @movie.rating %></i>
  <br /><br />
  <p><%= @movie.review %></p>
  <div class="float-right">
    <%= link_to 'Edit', edit_movie_path(@movie), class: 'btn btn-primary' %>
    <%= link_to 'Destroy', movie_path(@place), method: :delete, data: {confirm: 'Are you sure you want to delete this?'},class: 'btn btn-danger' %>
  </div>
</div>